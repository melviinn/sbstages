{% extends 'base.html.twig' %}

{% block title %}Hello StageController!{% endblock %}

{% block body %}
<h1>Stages :</h1>
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">DateDébut</th>
                <th scope="col">nbJours</th>
                <th scope="col">Animateurs</th>
            </tr>
        </thead>
    <tbody>
        {% for stage in stages %}
            <tr class="table-active">
                <td>{{ stage.id }}</td>
                <td>{{ stage.dateDebut }}</td>
                <td>{{ stage.nbJours }}</td>
                <td>{{ stage.animateurs.getPrenom }}</td>
            </tr>
        {% endfor %}
    </tbody>
    </table>
                
{% endblock %}
